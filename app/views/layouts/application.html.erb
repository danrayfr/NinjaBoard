<!DOCTYPE html>
<html class="scroll-smooth selection:bg-paleFern selection:text-ivory no-touchevents hydrated font-poppins">
  <head>
    <title><%= content_for(:title) || "Ninja Board" %></title>
    <%=
      favicon_link_tag asset_path('https://assets-global.website-files.com/64149f79022d0c5fc8ce46e8/642445ba8d2a51cbca21ff5d_SupportNinja%20Logo%20Ivory%20Icon%20256.png')
    %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="border-x-2 border-black bg-gray-50 flex flex-col min-h-screen font-poppins">
      
    <div aria-live="assertive" class="pointer-events-none fixed inset-0 flex items-end px-4 py-6 sm:items-start sm:p-6 z-50">
      <%= turbo_frame_tag 'flash', class: "flex w-full flex-col items-center space-y-4 sm:items-end" do %>
        <%= render 'layouts/flash' %>
      <% end %>
    </div>

    <%= render "layouts/navbar" if user_signed_in? %>

    <main class="flex-grow">
      <%= yield %>
    </main>

  </body>
</html>
