<!DOCTYPE html>
<html class="scroll-smooth selection:bg-black selection:text-ivory no-touchevents hydrated font-grotesque">
  <head>
    <title><%= content_for(:title) || "Ninja Board" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= "h-full bg-gray-50" if devise_controller? || user_signed_in? %>">
      <% if user_signed_in? %>
        <div>
          <%= render "layouts/sidenav" %>
          <main class="py-10 lg:pl-64">
            <%= yield %>
          </main>
        </div>
      <% else %>
        <main class="flex-grow">
          <%= yield %>
        </main>
      <% end %>
  </body>
</html>
