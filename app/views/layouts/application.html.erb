<!DOCTYPE html>
<html class="scroll-smooth selection:bg-paleFern selection:text-ivory no-touchevents hydrated font-outfit selection:text-offWhite">
  <head>
    <title><%= content_for(:title) || "Ninja Board" %></title>
    <%= favicon_link_tag asset_path('https://assets-global.website-files.com/64149f79022d0c5fc8ce46e8/642445ba8d2a51cbca21ff5d_SupportNinja%20Logo%20Ivory%20Icon%20256.png') %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= "h-full bg-gray-50" if devise_controller? || user_signed_in? %>">
      <div aria-live="assertive" class="pointer-events-none fixed inset-0 flex items-end px-4 py-6 sm:items-start sm:p-6 z-50">
        <%= turbo_frame_tag 'flash', class: "flex w-full flex-col items-center space-y-4 sm:items-end" do %>
          <%= render 'layouts/flash' %>
        <% end %>
      </div>
      
      <% if user_signed_in? %>
        <% if current_user.admin? %>
          <div>
            <%= render "layouts/sidenav" %>
            <main class="py-10 lg:pl-64"> 
              <%= yield %>
            </main>
          </div>
        <% else %>
          <%= render "layouts/navbar" %>
          <main class="flex-grow">
            <%= yield %>
          </main>
        <% end %>
      <% else %>
        <main class="flex-grow">
          <%= yield %>
        </main>
      <% end %> 
  </body>
</html>
